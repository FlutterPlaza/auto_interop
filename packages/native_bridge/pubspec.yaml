name: native_bridge
description: Runtime package for native_bridge - type-safe Dart bindings for native packages (npm, CocoaPods, Gradle).
version: 0.1.0
homepage: https://github.com/FlutterPlaza/native_bridge

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

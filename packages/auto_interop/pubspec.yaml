name: auto_interop
description: Runtime library for auto_interop — provides platform channel management, type conversion, and error handling for auto-generated native bindings.
version: 0.2.5
homepage: https://github.com/FlutterPlaza/auto_interop
repository: https://github.com/FlutterPlaza/auto_interop
issue_tracker: https://github.com/FlutterPlaza/auto_interop/issues
topics:
  - flutter
  - native
  - codegen
  - platform-channels
  - bindings

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

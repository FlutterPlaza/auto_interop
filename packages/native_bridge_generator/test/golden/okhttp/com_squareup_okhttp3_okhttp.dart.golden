// GENERATED CODE — DO NOT EDIT
// Generated by native_bridge_generator

import 'package:native_bridge/native_bridge.dart';

/// HTTP request methods.
enum HttpMethod {
  get,
  post,
  put,
  delete;
}

/// Represents an HTTP request.
class Request {
  final String url;
  final String method;
  final Map<String, String> headers;
  final String? body;

  Request({required this.url, required this.method, required this.headers, this.body});

  factory Request.fromMap(Map<String, dynamic> map) {
    return Request(
      url: map['url'] as String,
      method: map['method'] as String,
      headers: map['headers'] as Map<String, String>,
      body: map['body'] as String?,
    );
  }

  Map<String, dynamic> toMap() => {
    'url': url,
    'method': method,
    'headers': headers,
    if (body != null) 'body': body,
  };
}

/// Represents an HTTP response.
class Response {
  final int code;
  final String message;
  final String? body;
  final Map<String, String> headers;

  Response({required this.code, required this.message, this.body, required this.headers});

  factory Response.fromMap(Map<String, dynamic> map) {
    return Response(
      code: map['code'] as int,
      message: map['message'] as String,
      body: map['body'] as String?,
      headers: map['headers'] as Map<String, String>,
    );
  }

  Map<String, dynamic> toMap() => {
    'code': code,
    'message': message,
    if (body != null) 'body': body,
    'headers': headers,
  };
}

/// HTTP client for making network requests.
class OkHttpClient {
  static const _channel = NativeBridgeChannel('com_squareup_okhttp3_okhttp');


  /// Creates a new call for the given request.
  Future<Call> newCall(Request request) async {
    final result = await _channel.invoke<Call>('newCall', {
      'request': request.toMap(),
    });
    return result;
  }

  /// Closes the client and releases resources.
  Future<void> close() async {
    await _channel.invoke<void>('close');
  }
}

/// Represents an in-progress HTTP call.
abstract class Call {
  static const _channel = NativeBridgeChannel('com_squareup_okhttp3_okhttp');


  /// Executes the request synchronously.
  Future<Future<Response>> execute() async {
    final result = await _channel.invoke<Future<Response>>('execute');
    return result;
  }

  /// Cancels the request.
  Future<void> cancel() async {
    await _channel.invoke<void>('cancel');
  }
}


// GENERATED CODE — DO NOT EDIT
// Generated by auto_interop_generator
//
// Package: com.google.code.gson:gson@2.10.1
// Source: gradle
// Platform availability: Android only (Gradle/Maven package)

// ignore_for_file: unused_field, unused_import, camel_case_types, prefer_null_aware_operators

import 'package:auto_interop/auto_interop.dart';

/// Main class for JSON serialization and deserialization.
abstract interface class GsonInterface {
  /// Deserializes a JSON string into an object.
  Future<String> fromJson(String json);

  /// Serializes an object to a JSON string.
  Future<String> toJson(String src);
}

/// Main class for JSON serialization and deserialization.
class Gson implements GsonInterface {
  static final _channel = AutoInteropChannel('com_google_code_gson_gson');

  final String _handle;

  Gson._(this._handle);

  static Gson fromHandle(String handle) => Gson._(handle);

  static Future<Gson> create() async {
    final handle = await _channel.invoke<String>('Gson._create');
    return Gson._(handle);
  }

  /// Deserializes a JSON string into an object.
  @override
  Future<String> fromJson(String json) async {
    final result = await _channel.invoke<String>('Gson.fromJson', {
      '_handle': _handle,
      'json': json,
    });
    return result;
  }

  /// Serializes an object to a JSON string.
  @override
  Future<String> toJson(String src) async {
    final result = await _channel.invoke<String>('Gson.toJson', {
      '_handle': _handle,
      'src': src,
    });
    return result;
  }

  Future<void> dispose() async {
    await _channel.invoke<void>('_dispose', {'_handle': _handle});
  }
}

/// Builder for configuring Gson instances.
abstract interface class GsonBuilderInterface {
  /// Enables pretty printing.
  Future<GsonBuilder> setPrettyPrinting();

  /// Sets the date format pattern.
  Future<GsonBuilder> setDateFormat(String pattern);

  /// Builds the Gson instance.
  Future<Gson> create();
}

/// Builder for configuring Gson instances.
class GsonBuilder implements GsonBuilderInterface {
  static final _channel = AutoInteropChannel('com_google_code_gson_gson');

  final String _handle;

  GsonBuilder._(this._handle);

  static GsonBuilder fromHandle(String handle) => GsonBuilder._(handle);

  static Future<GsonBuilder> create() async {
    final handle = await _channel.invoke<String>('GsonBuilder._create');
    return GsonBuilder._(handle);
  }

  /// Enables pretty printing.
  @override
  Future<GsonBuilder> setPrettyPrinting() async {
    final result = await _channel.invoke<String>('GsonBuilder.setPrettyPrinting', {
      '_handle': _handle,
    });
    return GsonBuilder.fromHandle(result);
  }

  /// Sets the date format pattern.
  @override
  Future<GsonBuilder> setDateFormat(String pattern) async {
    final result = await _channel.invoke<String>('GsonBuilder.setDateFormat', {
      '_handle': _handle,
      'pattern': pattern,
    });
    return GsonBuilder.fromHandle(result);
  }

  /// Builds the Gson instance.
  @override
  Future<Gson> create() async {
    final result = await _channel.invoke<String>('GsonBuilder.create', {
      '_handle': _handle,
    });
    return Gson.fromHandle(result);
  }

  Future<void> dispose() async {
    await _channel.invoke<void>('_dispose', {'_handle': _handle});
  }
}

/// Represents a JSON element.
class JsonElement {
  static final _channel = AutoInteropChannel('com_google_code_gson_gson');

  final String _handle;

  JsonElement._(this._handle);

  static JsonElement fromHandle(String handle) => JsonElement._(handle);

  /// Returns this element as a string.
  Future<String> getAsString() async {
    final result = await _channel.invoke<String>('JsonElement.getAsString', {
      '_handle': _handle,
    });
    return result;
  }

  /// Returns this element as an integer.
  Future<int> getAsInt() async {
    final result = await _channel.invoke<int>('JsonElement.getAsInt', {
      '_handle': _handle,
    });
    return result;
  }

  /// Checks if this element is a JSON object.
  Future<bool> isJsonObject() async {
    final result = await _channel.invoke<bool>('JsonElement.isJsonObject', {
      '_handle': _handle,
    });
    return result;
  }

  Future<void> dispose() async {
    await _channel.invoke<void>('_dispose', {'_handle': _handle});
  }
}

